
let deviceWidth = device.width;
let deviceHeight = device.height;

console.log("开始")


// swipe(deviceWidth / 2, deviceHeight / 2, deviceWidth / 2, 0, 1000);
// let rvListChilds = className("android.support.v7.widget.RecyclerView").findOnce().children();
// className("android.support.v7.widget.RecyclerView").findOnce().children().forEach(function (child) {
//     log("孩子们", child.bounds());
// });

// let contain = className("android.view.View").descContains("好店签到").findOnce().bounds();

swipe(deviceWidth / 2, 680, deviceWidth / 2, 0, 1000);


console.log("结束")